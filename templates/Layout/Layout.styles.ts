import styled from "styled-components";

import { Header as StyledHeader } from "@/components/Header/Header";
import { Footer as StyledFooter } from "@/components/Footer/Footer";
import { SubHeader as StyledSubHeader } from "@/components/SubHeader/SubHeader";

export const Container = styled.main``;

export const Header = styled(StyledHeader)``;

export const SubHeader = styled(StyledSubHeader)``;

export const Footer = styled(StyledFooter)``;
